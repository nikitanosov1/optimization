import lab2_1.Solution;

class Main {
    public static void main(String[] args) {
        double a = 2.5;
        double b = 4.0;
        double eps = Math.pow(10, -5);
        //int n = 26;
        // double delta = Math.pow(10, -4);
        // lab1.DichotomieDto dto = lab1.Functions.dichotomie(a, b, eps, delta);
        //Function<Double, Double> myFunction = (x) -> {return Math.exp(x - 1 + 1 / (x - 2));};
        //lab1.GoldenSectionDto dto = lab1.Functions.goldenSection(a, b, eps, myFunction);
        //lab1.FibbonachiDto dto = Functions.fibbonachiSection(a, b, n);
        //System.out.println(dto);

        Solution.solve();

    }
}